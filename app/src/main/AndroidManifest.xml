<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PayPod">

        <!-- Splash Activity -->
        <activity
            android:name="com.example.paypod.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity android:name="com.example.paypod.MainActivity" />

        <!-- Declare HomeActivity and other activities here if needed -->
        <activity android:name="com.example.paypod.LoginActivity" />

        <!-- Home Activity -->
        <activity android:name="com.example.paypod.HomeActivity" />

        <!-- History Activity -->
        <activity android:name="com.example.paypod.HistoryActivity" />

    </application>
</manifest>
